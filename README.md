# imgserver-src

Source code for a local GPU image/video generation server (SDXL, LTX, Wan, Z-Image, TTS) built on diffusers.

Configuration is supplied at runtime via `configs/server_config.json` (gitignored) and environment
variables — see `config.py` for the supported variables and defaults.
